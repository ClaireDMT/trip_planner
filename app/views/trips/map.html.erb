<%= turbo_frame_tag "show_content" do %>
  <div style="width: 100%; height: 100vh;"
        data-controller="map"
        data-map-lodging-markers-value="<%= @lodging_markers.to_json %>"
        data-map-rental-markers-value="<%= @rental_markers.to_json %>"
        data-map-place-markers-value="<%= @place_markers.to_json %>"

        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
  </div>
<% end %>
